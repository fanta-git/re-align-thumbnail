import { SizeFormContents } from "@/types/form"

export const sizeFormDefaults = {
    columns: 10,
    rows: 10,
    outputWidth: 1600,
    outputHeight: 900,
    thumbnailWidth: 160,
    thumbnailHeight: 90,
} satisfies SizeFormContents
